# Sample workflow for building and deploying a Jekyll site to GitHub Pages
name: Deploy Jekyll with GitHub Pages dependencies preinstalled

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["master"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
                    - name: Update .NET SDK
  # You may pin to the exact commit or the version.
  # uses: martincostello/update-dotnet-sdk@65c5f402b15326dd12d7b1dac63abdbb53ed695c
  uses: martincostello/update-dotnet-sdk@v3.1.2
  with:
    # The GitHub access token to use to create a Pull Request for any SDK update to the repository.
    repo-token: 
    # The optional Git branch name to use.
    branch-name: # optional, default is 
    # The optional .NET release channel to download the SDK for (2.1, 3.1, 5.0, etc.).
    channel: # optional, default is 
    # If true, any existing pull requests superseded by any pull request opened by the action are closed.
    close-superseded: # optional, default is true
    # The optional Git commit message to use.
    commit-message: # optional, default is 
    # The optional Git commit message prefix to use if commit-message is not specified.
    commit-message-prefix: # optional, default is 
    # If true, will output a summary of any .NET SDK update to $GITHUB_STEP_SUMMARY.
    generate-step-summary: # optional, default is true
    # The optional path to the global.json file to update.
    global-json-file: # optional, default is ./global.json
    # The optional comma-separated label(s) to apply to Pull Requests generated by the action.
    labels: # optional, default is 
    # The optional GitHub repository to generate the pull request against.
    repo: # optional, default is ${{ github.repository }}
    # If true, .NET SDK updates which do not contain security fixes are ignored.
    security-only: # optional
    # The optional value to specify using the latest build of the specified quality in the channel.
    quality: # optional, default is 
    # The optional email address to use for the Git commit.
    user-email: # optional, default is github-actions[bot]@users.noreply.github.com
    # The optional user name to use for the Git commit.
    user-name: # optional, default is github-actions[bot]
    # If true, the action will not push changes to GitHub.
    dry-run: # optional
          
